template: true
valuesFilePath: values.yml

resources:
  - name: git_vijay
    type: GitRepo
    configuration:
      gitProvider: {{ .Values.myRepo.gitProvider }}
      path: {{ .Values.myRepo.path }}
      branches:     
        include: ^{{gitBranch}}$ 
